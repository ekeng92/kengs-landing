# Overnight Review — 2026-04-30

AEON Watch pass after Eric granted broad local authority for Keng's Landing updates.

## Strongest read

Keng's Landing is doing two jobs at once:

1. **Operate a real STR property well enough to protect family time and cashflow.**
2. **Pressure-test an STR finance product with real messy data before productizing.**

The board should explicitly serve both jobs. If it only tracks software work, the property drifts. If it only tracks chores, the product thesis drifts.

## Highest-leverage next moves

### 1. Seed the board with real launch/tax cards

The task board now has the right fields, but the initial operating cards still need to be created in the backend. Use `docs/task-board-operating-system.md` as the source list.

Best first 5 cards:

- Pull Freestone CAD land/building split
- Get golf cart bill of sale
- Categorize 94 pre-service expenses
- Build depreciation schedule sheet
- Generate CPA packet export

Why: these directly reduce tax-season risk and improve the product's proof point.

### 2. Treat the guest book gaps as guest-risk cards

`docs/GUEST-BOOK-OUTLINE.md` has several missing sections. The critical ones are not cosmetic:

- Check-in details
- Septic system rules
- Pet policy
- Quiet hours
- Emergency info
- Checkout thermostat setting

These should become board tasks tagged `guest`, `ops`, and `risk`.

### 3. Add an “Owner Morning Digest” view later

Eric said he can be forgetful. The board should eventually produce one compact daily/weekly digest:

- Today-friendly next action
- Waiting/blocker follow-ups
- One property ops item
- One product/tax item

Not a notification firehose. One short executive-function packet.

### 4. Keep “personal STR” and “product STR” visibly linked

The docs already describe product validation signals. Add a light feedback loop from real property work into product work:

- When a finance task is annoying, tag it `product-signal`.
- When a guest/ops issue repeats, tag it `template-candidate`.
- When CPA/export work requires manual reconstruction, tag it `tax-gap`.

This makes Keng's Landing more than a property; it becomes a live lab.

## Suggested board tags

- `tax`
- `guest`
- `ops`
- `maintenance`
- `listing`
- `finance`
- `product`
- `receipt`
- `risk`
- `product-signal`
- `template-candidate`
- `tax-gap`

## Tomorrow if the PC comes online

Once AEON Watch has a stable room, first automation target should be a weekly Keng's Landing sweep:

1. Pull open tasks.
2. Identify stale In Progress.
3. Identify Waiting cards with due/follow-up date reached.
4. Pick top 3 next actions by risk and context.
5. Send one digest only if useful.

## Quiet conclusion

The property already has enough structure to become a system. The missing piece is not more ideas; it is a reliable loop that turns loose threads into visible cards and stale cards into one next action.
